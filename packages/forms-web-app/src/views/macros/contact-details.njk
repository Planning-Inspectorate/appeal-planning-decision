{% macro contactDetails(details) %}
  <p class="govuk-body" data-cy="contact-details-name">{{ details.name }}</p>
  <p class="govuk-body" data-cy="contact-details-email">{{ details.email }}</p>
{% endmacro %}
