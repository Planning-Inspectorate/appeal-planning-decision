{% extends "layouts/main.njk" %}
{% from "govuk/components/error-summary/macro.njk" import govukErrorSummary %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}
{% from "govuk/components/button/macro.njk" import govukButton %},

{% set title="Is your appeal about a listed building? - Before you start - Appeal a householder planning decision - GOV.UK" %}
{% set selection = appeal.fullPlanningSection.listedBuilding %}

{% block pageTitle %}
  {% if errors %}
    {% set title="Error: " + title %}
  {% endif %}
  {{ title }}
{% endblock %}

{% block backButton %}
    {{ govukBackLink({
    text: "Back",
    href: backLink
 })}}
{% endblock %}

{% block content %}

  {% if errorSummary %}
    {{ govukErrorSummary({
      titleText: "There is a problem",
      errorList: errorSummary
    }) }}
  {% endif %}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <form action="" method="POST" novalidate>
        <div class="govuk-form-group {{ 'govuk-form-group--error' if errors }}">
          <span class="govuk-caption-l">Before you start</span>

          {{ govukRadios({
            id: "listed-building",
            name: "listed-building",
            fieldset: {
              legend: {
                html: '<legend class="govuk-fieldset__legend govuk-fieldset__legend--l">
                        <h1 class="govuk-fieldset__heading">
                          Is your appeal about a listed building?
                        </h1>
                      </legend>',
                isPageHeading: true,
                classes: "govuk-fieldset__legend--l"
              }
            },
            errorMessage: errors['listed-building'] and {
              text: errors['listed-building'].msg
            },
            items: [
              {
                value: "yes",
                text: 'Yes',
                attributes: { "data-cy":"answer-full-planning"},
                checked: selection === "full-planning"
              },
              {
                value: "no",
                text: 'No',
                attributes: { "data-cy":"answer-listed-building"},
                checked: selection === "listed-building"
              }
            ]
          }) }}

        </div>

        {{ govukButton({
          text: "Continue",
          type: "Submit",
          attributes: { "data-cy":"button-save-and-continue"}
        }) }}

      </form>
    </div>
  </div>

{% endblock %}
