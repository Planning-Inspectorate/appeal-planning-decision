const COOKIE_POLICY_KEY = 'cookie_policy';

const DEFAULT_COOKIE_POLICY = {
  essential: true,
  settings: false,
  usage: false,
  campaigns: false,
};

module.exports = {
  COOKIE_POLICY_KEY,
  DEFAULT_COOKIE_POLICY,
};
