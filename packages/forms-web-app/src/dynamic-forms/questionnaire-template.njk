{% extends "layouts/lpa-dashboard/main.njk" %}

{% block before_content %}
  {% if pageCaption %}
    <div class="govuk-grid-row">
      <div class="govuk-grid-column-two-thirds">
        <span class="govuk-caption-l">
          {{pageCaption}}
        </span>
      </div>
    </div>
  {% endif %}
{% endblock before_content %}

{% block after_content %}
  {% if showSkipLink %}
    <div class="govuk-grid-row">
      <div class="govuk-grid-column-two-thirds">
        <p class="govuk-body">
          <a href="{{skipLinkUrl}}" class="govuk-link">Skip</a>
        </p>
      </div>
    </div>
  {% endif %}
  {% if showBackToListLink %}
    <div class="govuk-grid-row">
      <div class="govuk-grid-column-two-thirds">
        <p class="govuk-body">
          <a href="{{listLink}}" class="govuk-link">Return to questionnaire homepage</a>
        </p>
      </div>
    </div>
  {% endif %}
{% endblock after_content %}
