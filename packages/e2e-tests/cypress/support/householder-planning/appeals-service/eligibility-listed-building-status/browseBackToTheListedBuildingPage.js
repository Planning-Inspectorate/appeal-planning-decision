import { clickBackLink } from '../back-link/clickBackLink';

export const browseBackToTheListedBuildingPage = () => {
  // prove that the entered data is retained during navigation
  // -> use the provided 'back' button
  clickBackLink();

};
