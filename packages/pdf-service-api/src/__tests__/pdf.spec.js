const logger = require('../lib/logger');

describe('Pdf API', () => {
  test('GET /api/v1/pdf/appeals/12345 - It responds with a pdf file', async () => {
    logger.debug('Hitting the JEST tests...');
  });
});
