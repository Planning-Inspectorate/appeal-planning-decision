{% macro contactDetails(details, email) %}
  <p class="govuk-body" data-cy="contact-details-name">{{ details.name }}</p>
  <p class="govuk-body" data-cy="contact-details-company">{{ details.companyName }}</p>
  <p class="govuk-body" data-cy="contact-details-email">{{ email }}</p>
{% endmacro %}
