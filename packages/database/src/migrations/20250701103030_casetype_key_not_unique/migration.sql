BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[CaseType] DROP CONSTRAINT [CaseType_key_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
