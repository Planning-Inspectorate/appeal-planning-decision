{
	"name": "@pins/business-rules",
	"version": "0.0.0",
	"description": "PINS technical and business rule validation logic",
	"private": true,
	"main": "src/index.js",
	"scripts": {
		"doc": "jsdoc -c jsdoc.conf.json",
		"format": "prettier --write './**/*.js'",
		"lint": "eslint ./",
		"lint:fix": "eslint ./ --fix",
		"test": "jest",
		"test:cov": "jest --coverage",
		"test:watch": "jest --watchAll --runInBand --coverage",
		"documentation": "jest --bail --coverage --coverageReporters=cobertura --reporters=default --reporters=jest-junit"
	},
	"author": "",
	"dependencies": {
		"date-fns": "^2.28.0",
		"yup": "^0.32.9"
	},
	"devDependencies": {
		"eslint": "^8.17.0",
		"eslint-config-prettier": "^8.5.0",
		"eslint-plugin-jest": "^24.4.0",
		"jest": "^28.1.1",
		"jest-junit": "^13.2.0",
		"prettier": "^2.1.2"
	}
}
