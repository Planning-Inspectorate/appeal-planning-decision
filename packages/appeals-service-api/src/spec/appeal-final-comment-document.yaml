components:
  schemas:
    FinalCommentDocument:
      description: A document linked to an appeal final comment
      type: object
      required:
        - id
        - commentId
        - documentId
      properties:
        id:
          type: string
          format: uuid
        statementId:
          type: string
          format: uuid
        documentId:
          type: string
          format: uuid

        # relations
        Document:
            $ref: '#/components/schemas/Document'