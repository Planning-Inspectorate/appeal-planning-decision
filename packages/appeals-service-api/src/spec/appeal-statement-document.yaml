components:
  schemas:
    StatementDocument:
      description: A document linked to an appeal statement
      type: object
      required:
        - id
        - statementId
        - documentId
      properties:
        id:
          type: string
          format: uuid
        statementId:
          type: string
          format: uuid
        documentId:
          type: string
          format: uuid

        # relations
        Document:
            $ref: '#/components/schemas/Document'