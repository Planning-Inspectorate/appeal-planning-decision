apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app.fullname" . }}-authentication-service-api
  labels:
    {{- include "app.labels" . | nindent 4 }}
spec:
{{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.authenticationServiceApi.replicaCount }}
{{- end }}
  selector:
    matchLabels:
      {{- include "app.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /metrics
        prometheus.io/port: {{ .Values.authenticationServiceApi.config.port | quote }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "app.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "app.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.authenticationServiceApi.image.repository }}:{{ .Values.authenticationServiceApi.image.tag }}"
          imagePullPolicy: {{ .Values.authenticationServiceApi.image.pullPolicy }}
          ports:
            - name: {{ .Values.authenticationServiceApi.service.name }}
              containerPort: {{ .Values.authenticationServiceApi.config.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.authenticationServiceApi.service.name }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.authenticationServiceApi.service.name }}
          env:
            - name: LOGGER_LEVEL
              value: {{ .Values.authenticationServiceApi.config.logLevel }}
            - name: SERVER_PORT
              value: {{ .Values.authenticationServiceApi.config.port | quote }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
